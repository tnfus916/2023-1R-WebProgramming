import React from 'react'
import ReactDOM from 'react-dom/client'
import './index.css'
import Worldcup from './Worldcup'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <Worldcup />
  </React.StrictMode>, 
  // 0509 시간에만 잠깐 꺼두겠다, 켜두는 것 권장
)
